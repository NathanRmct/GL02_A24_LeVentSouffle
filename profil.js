var profil = function(test /* ? */){
	this.test = test;	
	// A remplir : C'est le profil qui devra ensuite être transformé au format VCard
   }

   profil.prototype.equal=function(){
    //
	}
   module.exports = profil;